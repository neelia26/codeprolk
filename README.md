# CODEPROLK - Extended with Quiz Backend

Services:
- frontend: Vite React app
- backend: FastAPI
- postgres: PostgreSQL

Run with Docker Compose:

```bash
docker compose up --build
```

Environment variables are in `backend/.env.example`. Copy to `.env` and adjust as needed.
